// Isolated until business identity is managed in Settings. Do not invent contact
// information. These are current application values, not historical snapshots.
export const invoiceBusiness: { name: string; contactLines: readonly string[] } = {
  name: "Nexa",
  contactLines: [],
};
